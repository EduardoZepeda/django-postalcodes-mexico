============
Installation
============

At the command line::

    $ easy_install django-postalcodes-mexico

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-postalcodes-mexico
    $ pip install django-postalcodes-mexico
